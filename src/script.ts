const names = "MD Rakibul Islam Rakib";
const age = 26;
// console.log(name);

const myFunc = (names: string, age: number) => {
  console.log(`My name is ${names}
        My Age: ${age}
    `);
};
myFunc(names, age);
